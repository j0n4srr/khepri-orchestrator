apply plugin: 'java-library'
apply plugin: 'groovy'

dependencies {
    // Dependências internas do framework (referência via projeto local)
    implementation project(':framework:base')
    implementation project(':framework:entity')
    implementation project(':framework:service')
    implementation project(':framework:common')

    // Ferramentas de teste (necessário para GroovyScriptTestCase)
    implementation project(':framework:testtools')
}

task install {
    doLast {
        // Logica de instalacao
    }
}

task uninstall {
    doLast {
        // Logica de desinstalacao
    }
}